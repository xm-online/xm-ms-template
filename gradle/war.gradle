apply plugin: "war"

bootWar {
    mainClass = "com.icthh.xm.ms.template.MstemplateApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
